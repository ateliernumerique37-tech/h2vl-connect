rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // La règle universelle pour le développement sécurisé
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}