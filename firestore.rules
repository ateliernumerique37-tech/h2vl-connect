rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règle globale simplifiée : tout utilisateur authentifié peut lire et écrire
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}