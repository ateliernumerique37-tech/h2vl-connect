
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règle temporaire pour le développement :
    // Autorise la lecture et l'écriture pour tout utilisateur authentifié.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
